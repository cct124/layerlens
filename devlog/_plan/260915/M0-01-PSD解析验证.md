# M0-01 PSD 解析验证

## 目标与验收

在已完成的最小工程上验证 Rust PSD 候选解析器，形成可复现的样本能力矩阵与首版支持子集。具体数据语义以[PSD 数据与素材](../../../docs/03-PSD数据与素材.md)为准，样本和阶段出口以[实施与验收](../../../docs/06-实施与验收.md)为准。

- 用有来源说明和预期结果的真实、合成 PSD 样本核对文档、图层层级、文字与分段样式、坐标、合成预览及简单位图导出。
- 将第三方解析类型隔离在适配器内部，验证结果不改变 PSD 只读边界，也不将缺失值伪装为完整数据。
- 记录逐样本的能力、差异、限制及失败原因，依据证据决定首版支持子集和解析器版本。
- 记录环境、样本指纹、冷／热条件、耗时和峰值内存；验证稳定源数据、外部文件变化、损坏输入及资源上限处理。

## 状态与基准

- 状态：计划，尚未开始；本记录仅登记后续工程任务，没有 PSD 解析实现或验证结果。
- 分支：`dev`。
- 代码基准：接手时以包含 M0-00 与 M0-00a 的提交为基准，先核对 `git log`、当前分支和工作区变化，不将任务建立时的提交号作为永久基准。
- 前序结果：[M0-00 Tauri 工程初始化](../../_fin/260915/M0-00-Tauri工程初始化.md)与[M0-00a 提交格式检查](../../_fin/260915/M0-00a-提交格式检查.md)已完成本机验证；提交、远端同步及 CI 状态以 Git 历史、运行报告和交付消息为准。

## 已完成项

- 已登记任务目标、验收依据和接手入口。
- 没有安装或选定生产 PSD 解析依赖，没有宣称候选解析器符合首版要求。

## 验证

本任务尚未运行解析、导出、视觉对照或性能实验。M0-00 的 12 项测试验证脚手架及接口，不能作为 PSD 兼容性证据。

## 下一步与阻塞

1. 阅读[技术架构](../../../docs/02-技术架构.md)与本任务引用的数据、验收文档，检查 `crates/layerlens-core/` 的现有边界；预期确定最小解析原型入口，保持核心不依赖 Tauri。
2. 核验优先候选 `ag-psd-rs` 的版本、许可证、API 和实际可读取字段，补齐可复现的最小样本及预期值；预期形成第一份样本清单与候选差异记录，不先承诺选型成功。
3. 用最小样本实现解析与简单位图导出验证，逐步加入文字、层级、坐标和异常输入；记录缺失项与环境数据，预期输出能力矩阵和下一轮验证清单。

当前未提供实际 PSD 设计稿。可先整理合成或可公开样本与验证入口；真实 H5 设计稿的准确性验收仍需补齐有授权来源和预期结果的样本。私有设计稿不提交公共仓库。

## 过程记录

### 2026-09-15

- M0-00 完成本机验收后登记本任务，作为下一优先工程入口；尚未执行 PSD 原型开发。
- 沿用 `0905c44` 加本机未提交脚手架作为接手描述，未虚构本地提交或远端同步状态。
