# M0-01 PSD 解析验证

## 目标与验收

在已完成的最小工程上验证 Rust PSD 候选解析器，形成可复现的样本能力矩阵与首版支持子集。具体数据语义以[PSD 数据与素材](../../../docs/03-PSD数据与素材.md)为准，样本和阶段出口以[实施与验收](../../../docs/06-实施与验收.md)为准。

- 用有来源说明和预期结果的真实、合成 PSD 样本核对文档、图层层级、文字与分段样式、坐标、合成预览及简单位图导出。
- 将第三方解析类型隔离在适配器内部，验证结果不改变 PSD 只读边界，也不将缺失值伪装为完整数据。
- 记录逐样本的能力、差异、限制及失败原因，依据证据决定首版支持子集和解析器版本。
- 记录环境、样本指纹、冷／热条件、耗时和峰值内存；验证稳定源数据、外部文件变化、损坏输入及资源上限处理。

## 状态与基准

- 状态：进行中；元数据、保存时合成预览及文字分段样式已接入正式核心并完成公开回归和本机对照。2026-09-17 文字原文／区间／显式属性批次完成；物理单位、Photoshop 排版／颜色参考、复杂素材和规模预算仍待验证。
- 分支：`dev`。
- 本轮实施基准：`f768e42`，开始本批文字开发时工作区干净；用户授权执行下一步计划并接续中断工作，开发完成时为本机未提交修改。
- 本批交付授权：2026-09-17 用户明确要求提交当前工作区并推送 `origin/dev`；文字实现、公开样本、候选补丁与本记录一并交付，实际提交号及推送结果以 Git 历史和交付消息为准。该授权仅限当前批次，后续没有用户明确指示不得提交或推送。
- 前次兼容批次基准：`99459e9`；2026-09-16 中断接续时已有 15 个受跟踪文件改动及规范化模块、兼容测试、3 个新 PSD 和 `vendor/` 未跟踪文件，索引为空。该批后续由 `f768e42` 提交。
- 前轮交付：用户于 2026-09-16 明确授权提交当时工作区并推送 `origin/dev`；首轮结果已在 `99459e9`，不将该次授权沿用到后续开发。
- 前次兼容批次交付：2026-09-16 用户明确授权提交当时工作区并推送 `origin/dev`；实现、公开样本、候选补丁与本记录由 `f768e42` 一并交付，远端同步结果以当时交付消息为准，不将该次授权沿用到本批文字开发。
- 代码基准：2026-09-16 接手 `dev` 的 `748f9bf`，工作区干净；该提交已包含 M0-00、M0-00a 与开发记录日期整理。
- 前序结果：[M0-00 Tauri 工程初始化](../../_fin/260915/M0-00-Tauri工程初始化.md)与[M0-00a 提交格式检查](../../_fin/260915/M0-00a-提交格式检查.md)已完成本机验证；提交、远端同步及 CI 状态以 Git 历史、运行报告和交付消息为准。

## 已完成项

- 已核验优先候选的实际 crate 名为 `ag-psd`，精确引入 `0.3.0` 作为实验依赖；生产选型未完成。
- 独立核心新增 `psd/`：Windows 稳定源读取、PSD 输入预检、规范化元数据、按需合成预览与简单透明 PNG。第三方类型不暴露到调用方，未接入桌面或 MCP。
- 首轮新增不依赖候选解析器的合成样本生成器，4 个 PSD、独立像素／属性预期与 SHA-256；`fixtures:check` 纳入统一检查。来源与规范见[样本说明](../../../crates/layerlens-core/tests/fixtures/psd/README.md)。
- 新增 `inspect_psd` 命令行实验入口：只读源文件，向全新目录输出 PNG 和逐项报告，不覆盖已有文件；记录源指纹、元数据、限制及阶段耗时。
- 首轮实验准入限制为 PSD v1、RGB/8 位、RAW/RLE 和已审查的简单资源／标签。默认源文件 64 MiB、累计 16 Mi 像素、4096 条记录、64 层组嵌套；是保守保护阈值，不是实测性能预算或进程 RSS 硬上限。
- 首轮按用户授权只读测试私有真实稿 `tab-1.psd`，定位资源预算、未知图像资源和未实现渐变等阻塞；专项探针导出合成预览及 5 个原始通道样本，并独立校验合成 alpha 和不透明像素。当时只在 `.local/` 建立探针与私有产物，未放宽正式核心准入。
- 前次兼容批次在 `vendor/ag-psd/` 固定局部补丁并保留 MIT 来源：有界跳读与诊断、明确未实现能力的分项恢复、父区段边界、descriptor／EngineData 递归限制、延迟解码上下文与预算、蒙版字段顺序、TySh 原文保留。详细修改及上游来源见[补丁记录](../../../vendor/ag-psd/LAYERLENS-PATCHES.md)。没有改变依赖版本或关闭严格模式。
- 容器预检、候选适配与规范化按职责拆分；加入资源／像素计量、7 类图层、原始记录索引、ICC 指纹和逐项诊断。文档级附加块按四字节补齐，图层级仍按两字节；全局蒙版诊断保留真实源偏移。
- 前次兼容批次公开样本扩至 7 个，新增隐藏父组、未知资源、opaque ICC、RAW/RLE 合成 alpha、特殊字符／空白／CRLF 原文及文字矩阵、复杂类型降级；回归另覆盖连续全局附加块、非法填充和全局蒙版来源。
- CLI 新增显式源大小、累计像素、图层数、单次解码预算，以及 `--metadata-only`／`--preview-only` 模式；报告区分 exported／skipped／failed，保留配置和解析器标识。
- `npm run check:rust` 纳入 CLI 参数测试和 `npm run test:parser`。第三方源码保留上游格式；生成清单由 `fixtures:check` 逐字节核验，解决直接生成结果与 Prettier 格式冲突。
- 本批新增 `psd/text_model.rs`、`text.rs` 与 `text/properties.rs`，从保留的原始 EngineData 读取字符／段落样式；TySh 原文及矩阵保持不变。仅接受精确原文或原文多一个尾随 CR，分别校验两类 UTF-16 区间、完整覆盖及代理对边界，每类最多 16384 段。
- 显式属性附来源路径，仅继承同类别源文件明确提供的 `DefaultRunData`；无效局部值或未展开引用不会触发默认值回退。保留字体引用、RGB 值、字号、间距及段落属性，单位为 `unverifiedEngine`；不计算 CSS、不推断字体字重或可用性，样式仍为 `partial`。
- 候选补丁升级为 `ag-psd 0.3.0 + LayerLens patch 2`，保留解析后的 EngineData 和缺失 TySh 原文状态，严格拒绝非法 UTF-16 与无效／非有限 EngineData 数字。没有更改依赖版本；兼容解码器仍保留在候选内部，核心不采用其裁剪／换行／字体默认化结果作为样式依据。
- 公开样本扩至 10 个：新增独立 EngineData 编码、27 个语义用例以及 72／300／缺失 DPI 对照；新增 `psd_text.rs` 集成回归与文字核心边界测试。真实稿读取到 95 个文字层、204 个字符段和 108 个段落段，统计及限制见本批验证。
- 共用的 `Capability`／`Support` 移至 `psd/capability.rs`，供文档与文字模型引用，避免拆分后的相互依赖；公开导出路径和序列化结构不变。

## 验证

环境：Windows x64，Rust `1.96.0`，Node.js `24.19.0`，npm `11.17.0`。以下 52 项为首轮历史结果，真实稿专项测试及本轮接续验证分别见后文；尚未做 Photoshop 视觉对照或受控冷／热规模性能验收。

| 检查                                          | 结果                                                                                                                   |
| --------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| 合成样本可复现性                              | `npm run fixtures:check` 逐字节核对 PSD、预期清单与指纹，不写文件                                                      |
| 独立核心测试                                  | 29 项通过（16 项单元测试、13 项集成测试），覆盖精确 RGBA、负坐标、缺失预览、全部截断前缀、预算、稳定源及审查补充的边界 |
| CLI 成功路径                                  | RAW、RLE 各生成 2 项 PNG；无合成图生成 1 项独立素材并在报告中注明预览不可用                                            |
| CLI 失败路径                                  | RGB16 返回 `Unsupported` 且不创建输出目录；既有目录拒绝覆盖，已有报告 SHA-256 不变                                     |
| 统一格式／Clippy／类型／lint／测试／构建／DTO | `npm run check` 通过，含样本校验、Clippy、前端构建、DTO 一致性及 52 项测试（核心 29、IPC 3、前端 7、hook 13）          |
| 桌面／安装包／远端 CI                         | 本轮未执行；原型没有新增桌面 Command 或前端功能                                                                        |

本机输出在忽略目录 `.local/psd-raw-report/`、`.local/psd-rle-report/`、`.local/psd-no-composite-report/`。例如 RAW 322 字节、4×3 合成图、3×2 独立素材；首次 debug CLI 报告的固定源＋解析＋指纹约 0.656 ms，预览解码＋编码约 0.116 ms、图层约 0.062 ms。这是单次微型样本墙钟记录，未控制系统冷／热缓存、未测峰值内存，不能当作规模性能基线。

### 首轮能力矩阵

| 样本／能力                | 本轮结果                                                          | 限制                                                        |
| ------------------------- | ----------------------------------------------------------------- | ----------------------------------------------------------- |
| `bitmap-raw.psd`          | 尺寸、72 DPI、图层顺序、隐藏状态、负坐标及 PNG 逐像素符合独立预期 | 无 ICC、无复杂效果的微型合成稿                              |
| `bitmap-rle.psd`          | 与 RAW 的规范化属性及 PNG 像素一致                                | 样本为 literal runs；预检另覆盖 PackBits 重复、无操作与越界 |
| `bitmap-no-composite.psd` | 元数据和独立素材可用，预览明确不可用                              | 文件故意缺失末尾 Image Data，不声明完整规范 PSD             |
| `rgb16.psd`               | 明确拒绝，不伪装为 RGB8                                           | 候选的高位深能力仍待独立验收                                |
| 合成 alpha／额外通道      | 保留元数据和可用图层；该类预览拒绝                                | 候选懒解码丢失 global alpha 状态，额外通道语义未验证        |
| 文字／分段样式／变换      | 未实现，本轮相关附加块拒绝                                        | 不能从 Rust 字段存在推断原文保真                            |
| ICC／复杂效果／组独立导出 | 未支持；未知资源、标记和视觉依赖明确拒绝                          | 不用原始通道或截图冒充完整视觉素材                          |

### 候选与依赖评估

- 上游：[Vasyanator/ag-psd-rs](https://github.com/Vasyanator/ag-psd-rs)，实际 package `ag-psd`。已核验官方索引和发行归档 `0.3.0`（2026-09-06，未撤回），SHA-256 `6470c6a2f96a00a8d4154504020b34807c2282438c98f90f5069ef3f8cb0e7bf`，对应提交 `ff8754fb5dfb3b91f4e28dd37df98f6404f1dbcc`；锁文件保存发行校验值。
- MIT，与本项目许可兼容；Rust 1.85／edition 2024，运行依赖仅 `flate2`，无桌面或平台 SDK 依赖。维护历史较短（仓库创建于 2026-06-28，最近推送 2026-09-06），需要自有样本验证。上游样本不全部随代码 MIT 授权，因此本轮没有复制其设计稿。
- PNG 编码复用锁文件已有 `png 0.17.16`，指纹复用 `sha2 0.10.9`，均为 MIT OR Apache-2.0；无新增前端依赖，Cargo 仅新增候选包及核心依赖关系。只在 Windows x64 验证，不据纯 Rust 实现宣称跨平台兼容。
- 候选 `use_raw_data` 持有压缩通道副本，可在源字节和文件句柄释放后解码；不是零拷贝。懒解码不继承读取预算或严格模式，故在调用前核验 RAW 长度、RLE 行表及实际展开数量。
- 候选部分元数据按声明长度分配，位图预算不能约束这些分配；本轮只允许有界的已审查结构，未验证的附加块先拒绝。放宽准入前必须加入对应长度／语义验证，不能仅修改白名单。
- 源码核验发现文本路径会规范化换行并裁去末尾换行／调整样式长度；这可能与本项目保留原文的契约冲突。下一轮须以带 CR、LF、空白、emoji 和分段样式的样本验证，依据差异决定修补候选或替换，尚未决定生产选型。

### 真实稿专项测试：tab-1（2026-09-16）

用户提供并授权本地测试 `.local/psd-samples/tab-1.psd`，未授权公开设计内容。源文件 451,165,691 字节（430.27 MiB），SHA-256 为 `123d1debd26c0662211265b31b167f361072fe7af03c313f18e7edccae3d01bd`；测试后复核指纹一致，源稿未修改。样本、预览、通道 PNG、诊断代码与报告均被 `.local/` 忽略规则覆盖，不提交设计图或原文。

- 文档：PSD v1，5047×3517，RGB/8 位，72 DPI，4 通道，存在合成透明度。画布 17,750,299 像素，超过原型默认 16 Mi 像素；文件大小也超过 64 MiB。
- 结构：514 条记录中有 95 个组边界，规范化后 419 个节点（95 组＋324 内容层），最大组深度 4。95 文字、185 智能对象、28 形状、106 效果记录；这些特征可以重叠，不是相加互斥分类。
- 资源：33 个图像资源，包含 ICC；全局 `lnk2` 315,644,372 字节，占主要体积。按区段跳读可避免为结构盘点分配整个嵌入对象。全部图层矩形累计 55,948,148 像素，后续应区分源大小、嵌入对象、按需解码及缓存预算。

| 路径                                           | 实际结果                                                                | 结论                                                                  |
| ---------------------------------------------- | ----------------------------------------------------------------------- | --------------------------------------------------------------------- |
| 原 `inspect_psd` 默认入口                      | `ResourceLimit`，未创建输出目录                                         | 正式核心尚不能打开该稿                                                |
| 专项探针仅提高到 512 MiB／256 Mi 像素          | `Unsupported`：仅允许 ResolutionInfo 资源                               | 单纯调大阈值不足以兼容                                                |
| 候选 strict＋handler 报错                      | offset 46，`Unread section data`；对应首个资源 1028                     | 未消费的未知图像资源使严格解析中断                                    |
| 候选允许 section 剩余字节，但保留 handler 报错 | offset 906978，gradient vector content not yet supported                | 存在实际未移植的渐变能力，不是纯预算问题                              |
| 候选探索模式，跳过位图                         | 419 节点、95 文字、204 个显式字符样式段、95 个变换                      | 与独立结构数量吻合，但容错可能忽略其他字段，未验原文／样式保真        |
| 候选 eager 保存时合成图                        | 5047×3517 RGBA PNG 生成，未自行重绘图层                                 | 避开 lazy helper 丢失 global alpha 的已知问题；不等于复杂图层渲染通过 |
| 原始通道抽样                                   | 5 个简单可见图层的通道 PNG 解码成功，完整保留包括画布外部分的矩形       | 探索模式输出，不作为生产独立素材与效果验收                            |
| 独立 PackBits 对照                             | 14,068 行严格解码；全部 alpha 相同，9,670,654 个不透明像素的 RGB 零差异 | 验证保存时合成像素；4,739 个半透明像素的去白底语义及 ICC 仍待验证     |

专项代码直接依赖已锁定候选，完全位于 `.local/psd-tab-1/probe/`，用于区别正式核心限制与候选能力。探索模式关闭 `strict` 与 `throw_for_missing_features` 仅用于发现可读取内容，不能视为生产修复。正式源码、默认预算及拒绝行为均保持本轮测试前状态。统计仅输出数量、偏移、指纹及限制，不记录设计文字和图层名称。

运行环境为 Windows 11 x64（10.0.26200）、AMD Ryzen 9 7945HX、物理内存约 31.69 GiB，debug 构建。元数据探索解析约 112 ms，进程峰值约 440 MiB；合成预览读取＋解析／解码约 2.63 s，PNG 编码约 2.23 s，包含诊断统计的探针总计约 6.10 s、峰值约 547 MiB；5 项原始通道抽样总计约 2.24 s、峰值约 564 MiB。峰值取 Windows 进程 `PeakWorkingSet64` 的 25 ms 轮询观察值，独立进程设 45 s／900 MiB 终止阈值，均未触发。原文件已被先前测试读取，未清空或控制系统缓存，以上是单次探索记录，不是冷启动、release 或 UI 性能承诺。

独立 Python/Pillow 验证器按行解码原文件合成 RLE，耗时约 1.42 s、峰值约 184 MiB。所有 alpha 和不透明 RGB 与候选 PNG 完全一致；半透明位置每个 RGB 通道有 4,736 个原始值差异，候选执行了白色底色去除，仍需 Photoshop 参考确认。缩略图已人工检查可展示完整多画板内容；未有独立 Photoshop 导出参考，不能宣称视觉完全一致。

本地复现入口：先在仓库根目录将 `CARGO_TARGET_DIR` 设为根 `target/`，执行 `cargo build --offline --locked --manifest-path .local/psd-tab-1/probe/Cargo.toml`；随后运行 `.local/psd-tab-1/run-probe.ps1 -Mode <core-expanded|strict|tolerant|explore|preview|layers>`。探针报告和 PNG 使用新建文件语义，重新实验需选择新的报告目录；不会覆盖本次结果。结构／独立像素对照入口为同目录 `inventory.py`、`validate_composite.py`，参数见脚本入口。

产物目录 `.local/psd-tab-1/`：`inventory.json`、逐模式 JSON、`preview.png`、`preview-thumbnail.png`、5 个 `layer-sample-*.png`、`composite-validation.json`。本次只新增忽略目录下的实验和更新开发记录，沿用此前未提交实现；未重跑不受影响的全仓测试或桌面构建。

### 正式核心兼容接续验证（2026-09-16）

接续时核心 39 项测试已通过，但首次真实稿正式入口仍报 `PSD 附加块签名无效`。核对本地独立盘点，发现文档级附加块使用四字节补齐，而预检统一用了两字节；修复后增加 `8BIM`／`8B64`、1–4 字节载荷、连续块和非法填充的公开回归，不采用扫描签名或忽略严格检查的方式恢复。

| 检查                  | 本轮结果                                                                                                                        |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| 统一检查              | `npm run check` 通过；最后补充全局蒙版诊断偏移后重跑受影响的 `check:rust` 与文档格式检查                                        |
| 自有测试              | 核心 41（单元 19、原型集成 14、兼容集成 8）、CLI 3、桌面 IPC 3、前端 7、hook 13，共 67 项通过                                   |
| 候选补丁              | 267 项通过，其中 10 项为补丁专项回归；部分上游测试在外部样本缺失时提前返回，不能当作完整上游样本库验收                          |
| 生成与接口            | 7 个 PSD／manifest 逐字节可复现；Rust/TypeScript DTO 一致；rustfmt、Clippy、类型、lint、前端构建通过                            |
| CLI 成功／失败        | 复杂类型样本导出 1 张合成图，5 个独立素材明确跳过；既有目录拒绝覆盖且报告哈希不变；真实稿默认 64 MiB 限制仍拒绝且不创建输出目录 |
| 私有稿结构            | 正式入口得到 419 节点：95 组、95 文字、185 智能对象、28 形状、6 调整层、10 位图；逐节点边界、自身及有效可见性与独立盘点一致     |
| 私有稿文字            | 95 层读取到 TySh 原文与矩阵；11 层原文与候选归一化结果不同，保留原文并报告差异；未验收字符／段落样式                            |
| 私有稿合成图          | 导出 5047×3517 RGBA PNG，与前轮探针图逐通道全部像素零差异；前轮独立 RLE 验证的全部 alpha 和不透明 RGB 结论仍适用                |
| 源与隐私              | 源 SHA-256 与前轮一致；原稿、完整元数据报告和派生图片由 `.local/` 忽略，不纳入公开样本或提交                                    |
| 桌面／安装包／远端 CI | 本轮未运行；本轮没有新增桌面或 MCP 接口                                                                                         |

真实稿使用 `--max-file-mib 512 --max-total-pixels 80000000`，保留默认单次解码 128 MiB 和 4096 条记录。累计声明像素（画布＋图层＋蒙版）为 76,147,009；有 514 条原始记录及 11 类全局附加块。ICC 未转换，预览能力为 `partial`；3 个 `vscg` 渐变缺口保留到对应图层诊断。由于全局蒙版及未验证的图层／祖先视觉依赖，该稿暂有 0 项可正式独立导出的素材，不将之前原始通道探针的 5 张图片当作本轮素材验收。

本机 debug 实验：元数据模式解析约 12.15 s；预览模式固定源＋预检＋解析＋指纹约 11.92 s，解码与 PNG 编码约 4.88 s，总进程约 16.88 s，25 ms 轮询观察到的 Windows 峰值工作集约 569.14 MiB。独立进程的 45 s／900 MiB 终止阈值未触发。源文件此前已读取，未控制文件缓存；本轮预检校验所有 RAW/RLE 通道，与前轮只做探索解析的约 112 ms 不可直接比较。这些数据用于暴露当前开销，不是 release、冷启动或 UI 性能承诺。

复现时在仓库根执行（输出目录须不存在）：

```powershell
cargo run --locked -p layerlens-core --example inspect_psd -- .local/psd-samples/tab-1.psd .local/psd-tab-1-core-resume-metadata --max-file-mib 512 --max-total-pixels 80000000 --metadata-only
cargo run --locked -p layerlens-core --example inspect_psd -- .local/psd-samples/tab-1.psd .local/psd-tab-1-core-resume-preview --max-file-mib 512 --max-total-pixels 80000000 --preview-only
```

已保存的本地证据包括这两个目录内的 `report.json`、预览目录内的 `preview.png`／`comparison.json`、`.local/psd-tab-1-core-resume-preview-process.json`、`.local/psd-resume-cli-smoke/` 和 `.local/psd-resume-check-rust.log`。只读结构／像素对照脚本为 `.local/psd-tab-1/validate_core_resume.py`；依赖前轮 `inventory.json`、探针 PNG 和独立像素验证记录，比较结果使用新建文件语义。半透明去白底只证明与既有候选结果一致，仍缺独立 Photoshop 参考；ICC 未转换，未重新渲染复杂图层。

### 文字与分段样式验证（2026-09-17）

本批以 `f768e42` 为基准，落实用户已批准的文字验证计划。当前输出是核心及 CLI 的实验契约，没有新增桌面 Command、前端业务状态或 MCP 接口。区间、来源与降级规则见[文字与样式](../../../docs/03-PSD数据与素材.md#文字与样式)，公开输入及独立预期见[样本说明](../../../crates/layerlens-core/tests/fixtures/psd/README.md#enginedata-分段样式)。

| 检查          | 本批结果                                                                                                                                                          |
| ------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Rust 统一入口 | `npm run check:rust` 通过：样本可复现、rustfmt、工作区 Clippy、测试及 DTO 一致性                                                                                  |
| 核心与适配层  | 核心 47 项（单元 22、原型集成 14、兼容集成 8、文字集成 3）、CLI 3、桌面 IPC 3 通过                                                                                |
| 候选补丁      | 269 项通过，其中 12 项补丁专项；部分上游测试缺少外部样本时提前返回，不代表完整上游样本库验收                                                                      |
| 前端          | `npm run check:frontend` 通过：Prettier、类型、ESLint、7 项测试及 Vite 构建；本批未改动 hooks，未重跑 hook 测试                                                   |
| 独立文字预期  | 27 个文字层覆盖原文与矩阵、精确／单 CR 映射、字符与段落属性来源、缺失／非法／未支持降级；区间错误不污染另一类别                                                   |
| DPI 与边界    | 72／300／缺失 DPI 的文字输出完全相同；覆盖 16384 段上限、越界／溢出、UTF-16 代理对与组合字符，以及非法 EngineData 数字和 UTF-16 的完整 PSD 失败路径               |
| 样本保留      | 共 10 个公开 PSD 及 manifest 逐字节可复现；前次 7 个 PSD 未改动，新增预期不依赖候选写入器                                                                         |
| 私有稿文字    | 95 个文字层全部按原文附一个 CR 映射，得到 204 个字符段、108 个段落段；204 个字符段都有显式字体、字号与 RGB 值                                                     |
| 私有稿诊断    | 文字诊断为 528 项 `unsupported` 和 95 项 `unverified`，没有 missing、invalid、textMismatch 或 invalidRange；未读取属性仍明确保留限制                              |
| 新旧结果对照  | 源大小与 SHA-256 一致；419 节点顺序、ID、父节点、原始记录索引、名称、类型、边界、自身／有效可见性及不透明度一致；95 层原文、UTF-16 长度、矩阵及归一化差异标记一致 |
| 未执行验收    | Photoshop 字体／物理单位／排版／颜色参考、复杂素材、受控冷／热性能和峰值内存；本批未运行桌面界面、安装包或远端 CI                                                 |

源文件仍为 451,165,691 字节，SHA-256 为 `123d1debd26c0662211265b31b167f361072fe7af03c313f18e7edccae3d01bd`。复现入口如下，输出目录须不存在：

```powershell
cargo run --locked -p layerlens-core --example inspect_psd -- .local/psd-samples/tab-1.psd .local/psd-tab-1-text-260917-final --max-file-mib 512 --max-total-pixels 80000000 --metadata-only
```

本机报告保留在 `.local/psd-tab-1-text-260917/report.json` 与 `.local/psd-tab-1-text-260917-final/report.json`，均由 Git 忽略，未输出私有文字或图层名称到公开记录。首轮 debug 解析约 12.734 s，补充严格 TySh UTF-16 拒绝后约 12.616 s；这是源文件已读、未控制系统缓存的两次元数据运行，没有测量本批峰值工作集，不能解释为性能优化或受控基线。最终只移动共用能力类型，未改变报告语义；结构调整后重新执行 Rust 检查。

## 下一步与阻塞

1. 从 `psd/text.rs`、`text/properties.rs` 与 `tests/psd_text.rs` 继续：取得可授权的 Photoshop 文字属性／排版参考，核对字号、行距、字距、缩放矩阵及不同 DPI 的单位语义；在有独立证据后再定义派生单位。当前显式分段值可读取，但仍为 `partial`，不能据人工输入值推断实际排版。
2. 用可授权的 Photoshop 导出参考核对 ICC、半透明去白底、蒙版、渐变、智能对象及组素材；保持当前独立素材拒绝边界，逐特性增加预期和回归，再决定生产支持子集与解析器选型。
3. 从 `preflight.rs` 的全通道预检与 `PsdDocument::open` 固定源路径测量 release、受控冷／热和代表性规模；区分源字节、嵌入对象、元数据、按需解码、编码输出及未来缓存预算，形成有证据的 M0 出口后再进入 M1 查看器。

已提供首个真实 PSD，正式核心兼容与文字原始样式批次完成；当前缺口为独立 Photoshop 参考、文字物理单位／复杂素材预期及规模资源预算。完整 M0-01 尚未完成，继续保留本计划，不归档。本批文字实现、样本与记录按用户本次明确指示提交并推送 `origin/dev`，实际结果以 Git 历史和交付消息为准；私有设计稿和派生产物保持在被忽略的 `.local/`。

## 过程记录

### 2026-09-15

- M0-00 完成本机验收后登记本任务，作为下一优先工程入口；尚未执行 PSD 原型开发。
- 沿用 `0905c44` 加本机未提交脚手架作为接手描述，未虚构本地提交或远端同步状态。

### 2026-09-16

- 用户授权执行下一步开发；本轮以“样本基线＋最小解析适配器＋简单透明 PNG 验证”为可评审目标，在独立 Rust 核心运行，后续桌面和 MCP 复用同一能力。
- 并行核验候选解析器、建立不依赖待测解析器的合成样本，以及验证 Windows 稳定只读源数据；验证结果在完成后更新，不提前宣称选型成功。
- 初次 Cargo 查询因默认离线／失效代理未找到包，不能据此认定候选不存在；通过官方 sparse index 与发行归档交叉核验实际 package `ag-psd`。按本次开发授权下载依赖，仅在下载命令中调整网络环境，未修改全局配置。
- 输入预检覆盖候选懒解码的预算和截断容错缺口；独立审查发现缺失 RGB 通道、未知混合模式、非法剪贴值及非正 DPI 可能被候选默认化，补充拒绝与回归验证。ICC 在候选严格读取中未消费有效载荷，本轮明确不支持。
- Windows 源读取在持有只共享读取的文件句柄期间复制字节；已有写句柄返回 `SourceChanged`，后续按需解码使用候选拥有的压缩副本，源路径改写或替换不改变结果。
- 首次统一检查在沙箱内因 Vite 启动子进程 `EPERM` 中断；在正常权限下重新执行同一 `npm run check` 全部通过，未跳过检查或修改工具配置。文档链接和 `git diff --check` 同时核验。
- 本轮为本机未提交修改，未提交或推送；完整 M0-01 尚未完成，因此继续保留同一活动任务记录，不归档或复制新任务。
- 用户随后提供私有真实稿并授权测试。保留既有未提交实现，先验证正式入口拒绝，再在忽略目录使用有界流式扫描、独立进程候选探针和独立 RLE 对照，记录成功内容与已确认的失败原因。源文件指纹前后一致，未公开或改写设计稿；本次仅更新开发记录，未提交或推送。
- 用户明确授权提交当前工作区并推送，同时要求此后无明确指示不得提交或推送；约束同步写入 `AGENTS.md`。交付包含原型、公开合成样本与开发记录，私有 PSD 和派生产物继续仅存于被忽略的 `.local/`。沿用本轮已通过的 52 项测试与专项验证；最终提交执行只读索引格式 hook。
- 首轮由 `99459e9` 提交并推送后，用户授权执行下一开发任务。本轮并行改进候选内部读取边界、容器预检和独立回归样本，正式核心同步图层分类及能力诊断；优先验证真实稿元数据与保存时合成图，文字保真先建立最小证据。保持原稿只读，不提交或推送。
- 用户要求接续中断工作。核对工作区和活动日志后保留全部在途实现，发现并修复文档级附加块对齐、全局蒙版诊断偏移及生成清单格式检查冲突；将未接入统一检查的候选补丁／CLI 测试纳入实际入口，完成真实稿正式核心验证并更新 README。
- 首次 `npm run check` 在沙箱中再次因 Vitest/Vite 子进程 `spawn EPERM` 中断；正常权限下同一入口通过。后续只针对新增蒙版偏移回归重跑 Rust 检查，并检查文档格式、链接及最终 diff。没有跳过失败测试或修改全局工具设置。
- 用户要求按工作区内容编写提交信息并提交推送。核对索引范围与实际暂存差异后，提交本批实现、公开样本、候选补丁和开发记录并推送 `origin/dev`；该授权仅限本批，不沿用到后续开发。

### 2026-09-17

- 用户批准下一步文字开发计划，并要求继续中断工作；从 `f768e42` 接手并保留本批在途修改。优先实现原文与样式区间、独立预期及失败诊断，未扩大到查看器、ICC 或性能优化。
- 核对候选发现友好文字解码会处理换行、裁剪末尾、调整长度并补字体；因此保留解析后的原始 EngineData，在核心单独校验。只允许精确匹配或一个额外 CR，避免靠文本归一化猜测范围。
- 独立样本补充混合字符／段落、转义高字节、缺少 TySh、非法容器和字体引用等情况。开发过程中修正转义字符串、错误码引用和测试上下文问题；JSON 数值比较按数值语义核对 2 与 2.0，同时保留对象键、区间、来源与 null 的严格断言。
- 首次前端检查检出样本生成器的未使用变量，改为明确列出清单字段，确认生成字节不变。随后 Vitest/Vite 因沙箱子进程 `spawn EPERM` 中断，在正常权限下重跑同一入口通过；只读报告对照调用 Git 子进程也遇到 EPERM，同样重跑成功。未跳过测试或关闭 lint。
- 完成真实稿新旧报告只读对照，保留私有产物；最终评审将共用能力类型独立为无模型依赖的模块，消除文字模型与文档模型的相互引用，并重跑 Rust 检查。更新样本说明、产品数据契约、README、候选补丁记录及当前任务索引；本批仍为本机未提交修改，没有推送。
- 用户随后明确要求提交当前工作区并推送，并再次强调后续不得擅自提交推送。沿用本批已通过的 Rust／前端检查，核对实际索引范围后通过提交 hook 检查；具体提交号与远端同步结果在交付消息中报告，不将本次授权沿用到后续开发。
