# M2-03 主动资源清理与任务失效

## 目标与验收

在[选区与任务生命周期](../../../docs/04-选区与任务.md)约束下区分关闭标签、释放任务和主动清理。第一批先完成核心预检／确认、全修订失效与取消，再接桌面有界确认入口；不能将本批当作 M2 整体出口。

- 预检只读，返回计划不持有大文档强引用；列出标签、活跃任务、已知加载／预览、未规范化的潜在路径别名和存活修订。计划属于原核心实例，不接受外部构造或跨实例使用。
- 确认在同一状态锁内重新计算影响；变化后拒绝且无副作用。同一计划成功后重试幂等，不清理之后重新打开的文档。
- 关闭目标标签并失效其全部已发布修订、快照和活跃任务；保留有界任务终态及创建请求幂等记录，不变更其他文档。释放与失效不可混淆。
- 在途解析／预览接受取消后不能发布晚到成功；同步计算不硬中断，旧引用和已发布结果按所有者释放后才归还额度，不能宣称清理回执即内存归零。
- 核心分页读取检查失效，旧准备提交不能复活范围；已返回的数据无法撤销。所有大对象在状态锁外释放。
- 补齐核心竞态、跨修订、关闭后任务、重试、资源账本，以及 DTO 和前端失效状态展示回归。

## 状态与基准

- 进行中，2026-09-23 创建；第一批核心及终态适配已实现并提交推送，桌面清理命令／确认 UI 尚未接入，不归档。
- 分支 `dev`，基准 `3be224c`。用户要求先提交推送吸附改动再开发；吸附提交已推送 `origin/dev`，远端与本地完整哈希均为 `3be224c60856766f9185cbc0d0284df402a666ed`，开始本批时工作区干净。
- 提交状态：第一批实现与随附文档已生成 `53b4e65`（`feat(cleanup): 实现主动资源清理与任务失效`）并推送 `origin/dev`，远端与本地完整哈希均为 `53b4e651a7b2db62b7e141d1fb3fa160eeca56b9`。未重启或控制用户桌面，不宣称原生手测通过。

## 已完成

- `documents/cleanup.rs` 新增不可伪造、核心实例绑定的预检计划与原子确认；同计划并发确认／重试只执行一次，影响变化或选择版本耗尽无部分副作用。计划不保护大文档、不在核心无界保存。
- 已发布修订使用弱索引，包含关闭／重载后仍被任务或外部租约持有的旧修订。清理撤销快照查找、失效活跃任务、取消关联加载／预览并在锁外释放内部引用；普通关闭与释放语义保留，同路径新文档不受旧任务清理影响。
- 租约检查接口和有界内容读取增加协作式失效检查；借用型元数据访问不尝试撤销 Rust 借用，已发布 PNG 不删除。资源按最后引用释放计费，未增加依赖、预算或权限。
- Rust DTO 生成失效终态与结构化错误，前端逐字段兼容并显示“已失效”，禁止终态任务读取／复制；刷新终态清除旧内容和引用，创建回执不再覆盖稍后任务列表的真实终态。
- 同步 README、产品、架构、选区生命周期、验收清单和任务索引；明确当前只有核心清理 API，没有桌面清理按钮或 MCP／导出能力。

## 验证

- `cargo test --locked -p layerlens-core --lib`：100 项通过，较基线新增 14 项；另扩展选择版本耗尽回归。覆盖预检不固定资源、跨实例、全修订／关闭后任务、隔离新文档、重试／并发、失效读取与准备提交、终态幂等、排队／运行解析和预览取消、未规范化别名及源／快照／PNG 最后引用计费。
- 完整 `npm run check` 在沙箱外退出 0：前端 17 文件／156 项测试、格式／严格类型／ESLint／生产构建，12 个 PSD 样本复现、rustfmt、全工作区 all-targets Clippy、Rust 工作区测试与 doctest 入口、`inspect_psd` 示例、解析器 270 项、DTO 一致性、Windows 测量脚本 5 项及 hook 13 项均通过。桌面选区适配定向 9 项通过，含新增清理终态契约用例；两组前端定向回归 16 项通过。基线记录见[M2-02](../260920/M2-02-桌面选区与任务交互.md)。
- 完整检查后复核路径别名风险：补充未规范化候选集合的确认重检，清理定向 11 项通过，随后完整 `npm run check:rust` 再次退出 0；未修改已通过的前端实现。最终 `git diff --check`、仓库 Prettier 检查通过，8 份变更文档的 72 个本地链接与标题锚点有效。
- 未启动桌面应用、未操作窗口，不宣称新 Windows 原生验收、DPI、长时 RSS 或性能实测通过。

## 下一步与边界

1. 第一批核心与终态兼容已交接，后续开发基准为已推送的 `53b4e65`；继续实现前先核对远端、工作区和本活动任务记录。
2. 从 `src-tauri/src/workspace/` 的有界后台接入计划保管，加入大小受限的清理预检／确认 DTO；当前文档与已关闭文档任务均可发起，取消／过期释放计划，重复确认返回原回执。再接确认 UI，并处理所有已加载任务页的失效、预览／内容晚到、响应预算与会话退出，不只刷新任务第一页。
3. 原生验收资源清理、标题栏／面板与吸附，再评估 M2 整体出口；之后才进入 M3 素材导出和 M4 MCP。

## 过程记录

### 2026-09-23

- 用户授权提交推送吸附代码。提交前 fetch 核验无分叉，17 文件正常 pre-commit 检查通过，生成 `3be224c` 并推送核验；未绕过 hooks。此前产品检查无新改动，沿用已通过结果。
- 选择先实现 Rust 核心闭环而不是先加清理按钮。当前核心没有导出作业；本批取消对象仅现有加载、预览及在检查点可中止的读取，不宣称取消尚不存在的导出作业。
- 首轮测试编译发现新测试误用不存在的 `DocumentService::list_tasks`、未实现 `PartialEq` 的摘要整体比较及错误映射借用参数；改为实际 `SelectionHandle` 入口、逐字段不变量和正确借用，未为测试增加生产 API。别名测试最初使用 `..`，但 Windows absolute 会先折叠，改用已有测试同样的大小写别名以真正覆盖后台规范化。随后核心 100 项通过。
- Vitest 在沙箱内因 Vite 启动子进程 `spawn EPERM` 失败；请求提权后同组测试通过，没有修改测试配置或跳过失败项。生成器沿用 ts-rs 对 `deny_unknown_fields` 的既有解析警告，未隐藏。
- 完整检查后的自审发现：确认时的路径截止点可能包含预检后新入队、尚未规范化的别名。因此影响中补充潜在别名 ID 集合，变化即拒绝旧确认，仍只取消最终规范化后命中目标的作业。受控回归同时覆盖新别名要求重新确认和无关路径不被取消；未通过锁内文件访问来识别别名。
- 用户授权提交推送本批改动。提交前 `fetch` 核对 `dev` 与 `origin/dev` 无分叉；完整 `npm run check` 退出 0，pre-commit 暂存格式检查通过，生成 `53b4e65` 并推送至 `origin/dev`，随后核验本地与远端完整哈希一致。
