# AGENTS.md

## 项目与文档

- LayerLens 是基于 Rust + Tauri 的开源 PSD 解析与 AI 协作工具，通过交互式选区、样式提取、素材导出和 MCP，辅助 Agent 分块还原 H5 页面。
- 从 [README.md](README.md) 了解项目；`docs/` 维护设计与验收标准，`devlog/` 记录工程进展，按需建立目录。规划不代表已实现。
- Rust + Tauri 为已定技术方向；解析库、界面框架及 Agent 专属集成方案须经验证后确定。

## 接手与定位

- 以用户当前任务为准，先检查分支、提交和工作区改动，再读取相关目录的 `AGENTS.md`。
- 若已有 `devlog/README.md`，接手工程任务时先读索引和对应活动任务，再核对代码；继续开发且未指定任务时，从优先任务继续。
- 先检索、后读取、再编辑：用 `rg -n` 定位定义与相关引用，用 `rg --files` 查找文件，只读取相关区间及必要上下文。

## 修改与实现

- 只修改任务相关内容，保留已有改动；优先使用 patch 工具，避免整文件覆盖和顺手重构。
- 修改类型、接口或行为前检查调用关系；规则变化同步所属文档、引用及验收标准，避免重复维护。
- PSD 始终只读，界面、核心与 MCP 不提供设计内容编辑或保存回写；导出生成独立文件。界面布局遵循[产品与交互](docs/01-产品与交互.md)。
- Rust 核心统一管理多文档、各文档选区、活动标签与任务状态；桌面界面和 MCP 复用同一份状态及解析、导出能力。
- 当前选区与任务快照分开管理，切换选区不改变执行目标；Agent 高亮不覆盖用户选择。
- MCP 工具结果附带已提交选区及区域内容，超量部分按同一快照续读；预览和素材按需获取，保持 MCP 标准协议结构。
- 样式以 PSD 数据为依据，保留文字分段样式、变换和坐标单位；缺失或不支持的属性明确标记，不用推测值冒充原稿数据。
- 区分局部参考图与独立素材导出；大文件按需解码、缓存，避免阻塞界面或反复传输完整 PSD。

## 模块化与依赖边界

- 按业务职责和变化原因划分模块，依赖方向遵循[技术架构](docs/02-技术架构.md)：界面与协议适配层调用应用核心，核心不依赖前端组件、Tauri Commands 或 MCP 消息类型，第三方解析库通过适配器隔离。禁止循环依赖和绕过模块接口访问内部实现。
- 业务计算与文件、网络、界面等副作用分离；解析规范化、坐标转换、范围校验等逻辑应能独立验证。桌面与 MCP 复用应用服务，不分别实现同一业务规则。
- 模块默认隐藏实现细节，Rust 优先使用私有或 `pub(crate)` 可见性，仅公开调用方真正需要的接口。共享代码放入职责明确的模块，避免把无关逻辑集中在 `utils`、`common` 或单一服务中。
- 前端按功能组织组件、交互状态及核心调用；组件负责展示与交互，复杂业务流程不堆在页面或事件处理器中。前端可以保存草稿和视图状态，但不复制权威业务状态。
- 函数保持单一职责，模块拆分以可理解、可测试和可维护为依据。仅在实际复用、替换外部依赖或测试隔离需要时增加抽象；不为每个函数包装 trait，也不要求每个逻辑模块独立建 crate。

## 代码与接口规范

- 标识符使用清晰的英文，遵循语言惯例及项目术语；Rust 模块、函数与变量使用 `snake_case`，类型与 trait 使用 `UpperCamelCase`。避免含义不明的缩写、魔法数字和混用同义名称。
- 用类型表达领域约束：不同用途的 ID、坐标空间与单位在容易混淆的边界使用专用类型，状态使用枚举或可辨识联合，减少字符串状态和多个布尔值组合出的非法状态。
- 外部输入在边界校验，核心入口维护业务不变量；TypeScript 类型和反序列化成功不能替代运行时校验。DTO 与 Schema 采用单一来源生成或自动一致性检查；接口变化同步调用方、示例和契约测试，已发布接口需说明兼容或迁移方式。
- 采用惯用、直接的实现，控制嵌套和隐式副作用；常量与配置放在所属模块，默认值、单位和有效范围明确。生成代码修改其来源或生成器，不手工修补生成结果。
- 优先使用安全 Rust；确需 `unsafe` 或 FFI 时限制在最小封装内，写明必要性、安全不变量和调用前提，并验证边界。不得用 `unsafe`、无依据的类型断言或 `any` 掩盖模型与类型问题；TypeScript 工程初始化时启用严格类型检查。

## 注释与开发文档

- 项目说明与业务注释默认使用简洁中文，协议名、标识符和标准术语保留英文；修改既有文件时保持语言一致。注释随行为一起更新，删除失效注释和被注释掉的旧代码，历史由 Git 保留。
- 核心模块使用 Rust `//!` 或对应模块文档说明职责、依赖边界及重要不变量；公开接口使用 Rust `///` 或 TypeScript TSDoc 说明用途和调用契约。对简单内部函数不要求机械添加注释。
- 接口文档按需写明参数含义、单位与坐标空间、返回结果、错误条件、所有权、副作用及取消行为；Rust 文档按适用情况补充 `# Errors`、`# Panics`、`# Safety`。复杂公共 API 提供最小示例，可执行的 Rust 示例纳入 doctest。
- 行内注释重点解释设计原因、算法依据、兼容性处理和并发约束，不逐行翻译代码。非直观的 PSD 数据处理、UTF-16 索引、矩阵变换和缓存失效逻辑需说明依据，必要时链接规范或样本记录。
- 每处 `unsafe` 写明 `SAFETY` 说明，解释当前上下文如何满足安全条件；`TODO`／`FIXME` 需包含具体问题、后续动作及关联 issue 或任务记录，不留下没有背景的占位语句。影响设计或兼容性的决策归入所属文档与任务记录，不只藏在注释中。

## 错误、并发与资源管理

- 可恢复失败通过 `Result` 和可识别的领域错误返回，保留原因链和操作上下文，由适配层转换为用户提示或协议错误。禁止静默吞错、空 `catch`，或以默认值伪装成功；生产路径不对用户输入、解析或 I/O 失败直接使用 `unwrap`／`expect`，确属不变量时说明依据。
- 明确文档、文件句柄、缓存、订阅和后台作业的所有者及释放时机，优先使用所有权和 RAII；覆盖成功、失败、取消、断连及退出路径，不能依赖界面组件销毁来释放核心任务。
- 同步锁只保护短时状态访问，不跨 `await`、解析、解码或文件写入；耗时工作进入有界后台执行，不阻塞界面或异步运行时。新增并发需明确共享状态、提交顺序、取消传播和资源上限，不通过无限并发或反复重试掩盖问题。
- 采用结构化日志，记录操作、关联 ID、阶段耗时和错误，不在各层重复记录同一失败。默认不记录完整 PSD 文字、像素、凭据或其他敏感内容；性能优化需保留基线与对比结果。

## 工具链、依赖与配置

- 下列工具链与自动化要求在工程初始化时落实，并在 README 记录真实入口；当前仅有设计文档，不预设脚本或 CI 已存在。锁定 Rust 工具链及前端运行时、包管理器版本，声明实际支持的环境和必要系统依赖。
- 提交应用所需的 `Cargo.lock` 及前端锁文件，前端只使用一种包管理器；自动化采用锁定依赖的安装与构建方式。依赖升级与功能修改尽量分开，避免一次变更同时扩大行为和工具链差异。
- 新增依赖先检查现有能力，说明用途并评估维护状况、许可证兼容性、平台支持、构建与运行成本；只启用需要的功能。不为简单功能引入庞大框架，Git 依赖固定到可复现的版本或提交。
- 配置集中管理并提供必要的示例、默认值及校验；不硬编码个人绝对路径、端口或凭据。遵循 Tauri 最小权限原则，文件和网络能力按实际需求开放；密钥、私有设计稿、缓存和构建产物不提交仓库。

## 测试与持续集成

- 按风险选择测试层级：纯业务规则用单元测试，解析、文件导出、状态及接口边界用集成测试，关键桌面与 MCP 流程做端到端验证。新增重要行为和缺陷修复补充能检出错误的用例，不为机械或低风险修改堆积测试，也不以覆盖率数字代替有效断言。
- 测试验证公开行为和不变量，不绑定私有实现或只复述代码；重点覆盖边界值、错误恢复、并发顺序、取消与幂等。文件测试使用隔离临时目录和可复现样本，时间与随机性可控制，避免依赖真实网络、固定休眠或开发者本机环境。
- 工程建立后提供统一的格式、静态检查、测试和构建入口，本地与 CI 共用配置。Rust 纳入 rustfmt、Clippy、测试及适用 doctest，前端纳入格式、类型、lint、测试与构建；按实际工作区和支持的 feature 配置执行，不盲目组合互斥特性。
- CI 以 Windows 为基础验证环境，其他平台只有纳入实际构建与验证后才声明支持。提交前完成受影响范围的检查，合并前通过必需流水线；只有遇到新改动、失败或未解决风险时扩大或重复验证。
- 不通过全局关闭 lint、跳过失败测试或更新快照来掩盖问题。确需局部豁免时限定范围并注明原因；生成类型或资源时检查可重复生成，依赖与许可证问题纳入自动化检查或明确记录的升级评审。

## 验证与交付

- 修改后检查 diff；文档检查链接与一致性，代码按仓库实际配置运行相关格式、类型、lint、测试或构建检查。
- 解析与导出改动使用有代表性的 PSD 验证；界面与 MCP 联动改动检查状态一致性。已通过的检查仅在新改动或问题需要时重跑。
- 交付说明改动、验证结果及未完成项；区分静态检查、运行验证、视觉验收和性能实测。未经验证的兼容性与性能保持“待验证”。
- 每次变更聚焦一个可评审目标；提交或 PR 说明问题、最终行为、验证证据及兼容性影响，评审同时检查模块边界、复杂度、并发和失败路径。发布时同步版本、变更说明及适用的迁移提示，执行打包与安装启动验证；具体流程在工程建立后记录。
- 提交与推送按用户授权执行，明确区分本机修改、本地提交和远端同步。

## devlog 交接

- 只记录工程实现、工具链、测试、性能实验和故障排查；纯文档工作无需记录，工程任务附带的文档更新并入该任务。
- 使用同仓库普通 Git 文件和相对路径。`devlog/README.md` 只维护当前阶段、活动任务链接及优先顺序。
- `devlog/_plan/<任务名>.md` 每任务一份：顶部维护目标与验收、状态、分支与代码基准、已完成项、验证、下一步及阻塞；后面按日期追加关键过程和决策原因。
- 阶段完成、遇到阻塞或交接前更新记录；下一步注明入口、动作和预期结果，保留失败尝试、验证环境及相关未提交文件，结论须与代码一致。
- 完成、取消或被替代的任务写明结果，移入 `devlog/_fin/` 并更新索引；实现与记录尽量一起提交，交接时注明本机、本地提交及远端同步状态。

## 参考规范

以下资料用于理解原则，具体执行以本项目约定和已落地的工具配置为准，不直接套用其他仓库的完整流程。

- [Rust API Guidelines](https://rust-lang.github.io/api-guidelines/checklist.html)：命名、类型安全、接口边界、错误与 API 文档。
- [Google Engineering Practices：代码评审关注点](https://google.github.io/eng-practices/review/reviewer/looking-for.html)：设计、复杂度、测试、命名、注释与文档维护。
